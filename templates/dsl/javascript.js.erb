const base = require('../../../runners/framework/base.js');

class <%= class_name %> {
<% methods.each do |name, default| %>
  <%= name %>(val) {
    if(<%= default %>){
      baz = <%= name %>_foo;
    } else {
      baz = "<%= default %>";
    }

    baz.foreach(function(b) {
      z(a(b + 5) + 10).other().stuff();
    });

    inst = new OtherClass(baz);
    if (baz.ten()) {
      inst.var = "val";
    } else {
      inst.var = baz;
    }

    inst.do_a_thing();
    inst.and_another();

    <%= name %>_bar(inst);
  }
<% end %>
}

base.dslize(<%= class_name %>, {
<% methods.each do |name, default| %>
  <%= name %>: "<%= default %>",
<% end %>
})

module.exports = <%= class_name %>;
