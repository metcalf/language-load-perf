module DSLPerf
  class <%= class_name %> < Base
  <% methods.each do |name, default| %>
    def <%= name %>
      if <%= default %>
        baz = <%= name %>_foo
      else
        baz = "<%= id %>"
      end

      <%= name %>_bar(baz)
    end
  <% end %>
  end
end
